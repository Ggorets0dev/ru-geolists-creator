syntax = "proto3";

package geo_release;

message ReleaseNotes {	
	enum SourceType {
	        DOMAIN = 0;
		IP = 1;
  	}
	
        message FilesPaths {
	        string domain_list = 1;
		string ip_list = 2;
	}

	message Source {
    		string section = 1;
    		SourceType type = 2;
	}
	

	string time = 1;
        repeated FilesPaths files_paths = 2;
	repeated Source sources = 3;
}
